@AGENTS.md

Claude Code additionally loads `skills/*/SKILL.md` natively when this repo (or a symlink of `skills/<name>` into `.claude/skills/`) is in scope. Prefer the skill's `SKILL.md` index, then its `operations/` files.
